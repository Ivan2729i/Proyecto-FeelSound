Hola, {{ user.get_full_name|default:user.get_username }}

Recibimos una solicitud para restablecer la contraseña de tu cuenta en {{ site_name|default:"FeelSound" }}.

Para continuar, visita este enlace:
{{ protocol }}://{{ domain }}/accounts/reset/{{ uid }}/{{ token }}/

Si tú no solicitaste este cambio, ignora este mensaje.

— El equipo de {{ site_name|default:"FeelSound" }}
